// The dart:io library provides I/O (Input/Output) support.
import 'dart:io';

main() {
  //readLineSync() method (function) reads a line from the input.
  // stdin stands for standard input 
  print("Hello"+ " " + stdin.readLineSync()!);

}
